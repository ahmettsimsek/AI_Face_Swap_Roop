

Gerekli Bağımlılıkları Yükleyin:

Klonladığınız roop dizinine gidin:

bash
Kopyala
Düzenle
cd roop
Bağımlılıkları yüklemek için:

bash
Kopyala
Düzenle
pip install -r requirements.txt
Model Dosyasını İndirin:

inswapper_128.onnx dosyasını indirip roop dizinine yerleştirin.

Programı Çalıştırın:

Kurulum tamamlandıktan sonra, programı başlatmak için:

bash
Kopyala
Düzenle
python run.py
Notlar:

İlk çalıştırmada, program bazı modelleri indirecektir; bu işlem internet bağlantı hızınıza bağlı olarak zaman alabilir.​

Eğer bir hata alırsanız, hata mesajını dikkatlice inceleyip gerekli düzeltmeleri yapmanız gerekebilir.​

Daha fazla bilgi ve detaylı yönergeler için resmi GitHub deposunu ziyaret edebilirsiniz. 

Evet, doğru anladınız. Bu araç komut satırı üzerinden belirtilen parametrelerle çalışıyor. Paylaştığın ekran görüntüsündeki parametreler, aracın hangi dosyaları kullanacağını ve hangi özelliklerle çalışacağını belirlemek için kullanılır.

Örneğin, temel bir kullanım şekli:

bash
Kopyala
Düzenle
python run.py -s kaynak.jpg -t hedef.mp4 -o cikti.mp4
-s veya --source: Kaynak yüz görselini belirtir (yüzün alınacağı görsel).

-t veya --target: Yüzün yerleştirileceği hedef video/görsel dosyasını belirtir.

-o veya --output: İşlemin sonucunu kaydedeceğiniz dosyayı belirtir.

Diğer parametreler (örneğin fps, ses çıkarma, çıktı kalitesi, encoder vb.) daha gelişmiş ayarlar içindir ve isteğe bağlı olarak kullanılabilir.

Bu parametreleri komut satırına yazdığınızda Roop, işlemi başlatıp belirttiğiniz dosyaları işleyerek sonucu oluşturur.

Eğer yüklediğin kaynak görsel PNG formatındaysa ve çıktı olarak yine PNG dosyası istiyorsan, komut şu şekilde olmalıdır:

bash
Kopyala
Düzenle
python run.py -s kaynak.png -t hedef.png -o cikti.png
Burada:

kaynak.png: Yüzü alacağın PNG resim.

hedef.png: Yüzü yerleştireceğin hedef PNG resim.

cikti.png: İşlem sonrası oluşacak çıktı PNG resim.

Roop aracı, kaynak ve hedef dosyaları aynı türde olursa, çıktı olarak genellikle aynı türde çıktı verir. PNG formatında giriş sağlarsan PNG formatında çıktı elde edersin.

Eğer hata alırsan veya farklı formatta çıktı üretilirse, açıkça belirtmek için aşağıdaki ek parametreleri kullanabilirsin:

bash
Kopyala
Düzenle
python run.py -s kaynak.png -t hedef.png -o cikti.png --temp-frame-format png

✅ Doğru Komut (Sadece PNG ile)
Eğer:

kaynak.png = Yüz alınacak kişi

hedef.png = Yüzün yapıştırılacağı görsel

cikti.png = Çıktı dosyası (yeni yüzle)

O zaman doğru komut şu olacak 👇

bash
Kopyala
Düzenle
python run.py -s kaynak.png -t hedef.png -o cikti.png --temp-frame-format png --temp-frame-quality 100 --many-faces --keep-frames --execution-provider cpu --execution-threads 4


--------------------------------------

C:\Users\Smart-Pc\Desktop\roop>python run.py -s kaynak.jpg -t hedef.jpg -o cikti.jpg --temp-frame-format jpg --temp-frame-quality 100 --many-faces --keep-frames --execution-provider cpu --execution-threads 4

----------------------------------------------------


